name: "Format Check"

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  format-check:
    name: "Format Check"
    uses: "SciML/.github/.github/workflows/format-check.yml@v1"
